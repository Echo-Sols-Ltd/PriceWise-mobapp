export interface Product {
    id: string;
    name: string;
    owner: string
    price: number;
    category: string
    liked: boolean
    image: string
    description: string;
    rating: number
    createdAt: string;
    updatedAt: string;
}